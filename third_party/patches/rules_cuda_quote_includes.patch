diff --git a/cuda/private/toolchain_configs/nvcc.bzl b/cuda/private/toolchain_configs/nvcc.bzl
--- a/cuda/private/toolchain_configs/nvcc.bzl
+++ b/cuda/private/toolchain_configs/nvcc.bzl
@@ -204,7 +204,10 @@ def _impl(ctx):
                 ],
                 flag_groups = [
                     flag_group(
-                        flags = ["-I", "%{quote_include_paths}"],
+                        # Repository roots are quote-only paths. Treating them
+                        # as -I can shadow standard headers (for example an
+                        # SDK's plain-text `version` file shadows <version>).
+                        flags = ["-Xcompiler", "-iquote,%{quote_include_paths}"],
                         iterate_over = "quote_include_paths",
                     ),
                     flag_group(
