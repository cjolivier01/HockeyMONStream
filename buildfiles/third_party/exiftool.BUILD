# ExifTool is pure Perl; ship the pinned runtime without a platform-specific build.
filegroup(
    name = "runtime",
    srcs = glob(["lib/**"]) + ["exiftool", "LICENSE"],
    visibility = ["//visibility:public"],
)
