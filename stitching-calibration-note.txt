so, if I go to stitching calibration and change control point count (maybe this is something should be represented in the private config) and then play, I'd expect it to do the equivalent of ../hm's stitch.py --clean --game-id <game-id>, thus cleaning out all dependent stitching stuff in order to reconfigure stitching. but right nowm, it just says stitching is alreadt configured. the "CP xxxx" should by default, represent what's already in the provate config, or the default otherwise (we should set that somewhere in our code as maybe 1500, wdyt?).
