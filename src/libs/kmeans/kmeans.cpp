#include "kmeans.h"

namespace hm {
namespace kmeans {

void compute_kmeans(
    const std::vector<float>& points,
    int numClusters,
    int dim,
    int numIterations,
    std::vector<int>& assignments) {
  //
}

} // namespace kmeans
} // namespace hm
