\\mainpage hstream API Documentation

Welcome to the autogenerated API documentation for the hstream project.

This site is generated from the C++ sources using Doxygen. It includes
source browsing and cross-reference information to help you navigate the code.

Quick links
- Applications: see namespaces, classes, and files under `src/apps/`
- Libraries: see `src/libs/` for core components (camera, stitching, scoreboard, etc.)
- Plugins: see `src/gst-plugins/` for GStreamer integrations

Build instructions
- Build the docs: `bazelisk build //docs:site`
- Open the site: `bazel-bin/docs/site_html/html/index.html`

\\section structure Project Structure
- Apps live in `src/apps/` and are entry points wiring pipelines and services.
- Libraries live in `src/libs/` and provide reusable functionality.
- GStreamer plugins live in `src/gst-plugins/`.

\\section refs Cross-References
The documentation enables reference and referenced-by relations between
entities. Use the navigation pane to explore where symbols are defined and used.

